<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Log level defaults to INFO; override with -Dlog.level=WARN in CI -->
    <logger name="eu.kafkaesque" level="${log.level:-INFO}"/>
    <logger name="org.testcontainers" level="${log.level:-INFO}"/>
    <logger name="org.apache.kafka" level="WARN"/>

    <root level="${log.level:-INFO}">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
